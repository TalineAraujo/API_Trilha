  // const validarCPF = (cpf) => {
        //     if (!Number.isInteger(cpf))
        //         return false;
        //     if (cpf.length !== 11 || !!cpf.match(/(\d)\1{10}/)) 
        //         return false;